算法：
bbdis（暴力比较）：B&B算法，目前支持将属性划分为两种：纯数字列、字符串，
sindydis（倒排）：Sindy算法

参数介绍：
args(0): yes/no 代表本地模式/集群模式
args(1): 输入文件夹路径
args(2): 输出文件夹路径(可为空，则不落盘写入)

本地运行方式：
    配置参数为：yes file:///User/Data/ 运行即可。
集群运行方式：
    在home_litianfeng目录下，运行：
        spark-submit --driver-memory 2G --executor-memory 4G
    其中，jar包存储在当前目录下，数据存储在hadoop上。


数据：
1. scop.txt	3M
2. adult.data adult.test  5.3M
2. main.py 生成lhs.csv rhs.csv  900M
